import React from 'react';
import './helpers.css';

const Spinner = (props) => (
    <div className="j-container-helper">
        <div className="lds-hourglass"></div>
    </div>    
);

export default Spinner;